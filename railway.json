{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "cd commongrounds && python -c \"import os; print('PGDATABASE=', os.getenv('PGDATABASE')); print('PGHOST=', os.getenv('PGHOST')); print('DEBUG=', os.getenv('DEBUG'))\" && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn commongrounds.wsgi --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
