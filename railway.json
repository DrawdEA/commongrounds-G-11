{
  "$schema": "https://railway.com/railway.schema.json",
  "deploy": {
    "startCommand": "cd commongrounds && python manage.py migrate && python manage.py collectstatic --noinput && gunicorn commongrounds.wsgi --bind 0.0.0.0:$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
